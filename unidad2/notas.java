public class notas {
    public static void main(String[] args) {
        
        /*
        Método String.format: 
        String cadena = String.format("Se mostrará el primer parámetro %s, 
        se mostrará el segundo parámetro %f", paramétro1, parámetro2);

        %d - int
        %s - String
        %f - double

        System.out.println(String.format("IMPRESION")) = System.out.printf(IMPRESION);



        SOBRECARGA DE MÉTODOS
        Puede haber 2 métodos que se llamen igual, incluso que tengan el mismo valor de retorno
        Lo que los diferencia son los parámetros y el tipo de parámetros que reciba

        SOBRECARGA DE CONSTRUCTORES
        Se pueden crear varios constructores para que haya ojbjetos de la misma clase que puedan
        tener atributos diferentes.

        >>Método set<<
        Asigna el parámetro al atributo especificado
        Es un método vacío ya que únicamente asigna la entrada del usuario.
        Truco: Si escribes "set" en Visul Studio Code te autocompleta el método
        Ejemplo:
            public void setStock(int stock) {        
                this.stock = stock;
            }


        >>Método get<<
        Devuelve el atributo especificado. Este debe ser guardado en una variable o mostrarse.
        Truco: Si escribes "get" en Visual Studio Code te autocompleta el método
        Ejemplo:
            public String getNombre() {
                    return nombre;
                }
        
         */
        
    }

    
}
